= simple_form_for(@activity, html: { class: 'form-horizontal' }) do |f|
  = f.input :title, wrapper_html: { style: 'margin: 0' }
  %br
  = f.label :description
  = f.cktext_area :description

  %br
  = f.button :submit
