.container
  .row
    .col-lg-12
      %h1 Activities
      %hr

      - @activities.each do |activity|
        .activity
          = link_to activity, class: 'title-link' do
            %h2= activity.title
          %em
            Posted by
            = activity.user.name
            on
            = activity.created_at.strftime('%e %b %Y')
          %p= truncate_html activity.description.html_safe, length: 1200, omission: (link_to '.. read more', activity)
          %hr

