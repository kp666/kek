.container
  .row
    .col-lg-12
      %h2= @activity.title
      %em
        Posted by
        = @activity.user.name
        on
        = @activity.created_at.strftime('%e %b %Y')
      %p= @activity.description.html_safe
